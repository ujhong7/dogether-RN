// MARK: - Route: /group-create
//
// 역할: 그룹 생성 플로우 route와 src/screens 구현을 연결합니다.

import { GroupCreateScreen } from '../src/screens/groupCreate';

// 파일 이름의 kebab-case(group-create)는 URL/route 이름에 맞춘 표기입니다.
// 실제 코드 폴더(src/screens/groupCreate)는 TypeScript에서 자주 쓰는 camelCase 이름을 사용합니다.
export default GroupCreateScreen;
