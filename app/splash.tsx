// MARK: - Route: /splash
//
// 역할: 앱 시작 판단 화면 route와 src/screens 구현을 연결합니다.

import { SplashScreen } from '../src/screens/splash';

// "/splash" route 파일입니다.
// 실제 UI와 로직은 src/screens/splash/SplashScreen.tsx에 두고, app 폴더에서는 route 연결만 담당합니다.
export default SplashScreen;
